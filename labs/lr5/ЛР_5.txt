
#include <iostream>
#include <math.h>


using namespace std;


int main()
{
    setlocale(LC_ALL, "russian");
    std::cout << "Привет мир!\n";


    // For 

    // --------------
    // For1. Даны целые числа K и N (N > 0). Вывести N раз число K.

    /*
    int K, N;

    cout << "Введите целое число K=";
    cin >> K;
    cout << "Введите целое число N=";
    cin >> N;
    for(N; N > 0; N--) {
        cout << "\n #" << N << " " << K;
    }
    */


    // --------------
    // For2. Даны два целых числа A и B (A < B). Вывести в порядке возрастания все
    // целые числа, расположенные между A и B(включая сами числа A и B), а
    // также количество N этих чисел.

    /*
    int A, B;

    cout << "Введите целое число A=";
    cin >> A;
    cout << "Введите целое число которое больше А, B =";
    cin >> B;
    for (A; A < B + 1; A++) {
        cout << A << " ";
    }
    */

    // --------------
    // For3. Даны два целых числа A и B (A < B). Вывести в порядке убывания все
    // целые числа, расположенные между A и B (не включая числа A и B), а также количество N этих чисел.


    /*
    int A, B;

    cout << "Введите целое число A=";
    cin >> A;
    cout << "Введите целое число которое больше А, B =";
    cin >> B;
    B--;
    A++;
    cout << "\n Всего выведено чисел :" << B - A  + 1<< "\n";
    for (B; A <= B ; B--) {
        cout << B << " ";
    }
    */



    // --------------
    // For4. Дано вещественное число — цена 1 кг конфет. Вывести стоимость 1,2, . . ., 10 кг конфет.

    
    /*
    float A;

    cout << "Введите стоимость кг конфет =";
    cin >> A;

    for (int count = 1; count <= 10; count++) {
        cout << "Стоимость " << count << "кг. конфет " << count * A << "\n";
    }
    */


    // --------------
    // For5 Дано вещественное число — цена 1 кг конфет.Вывести стоимость 0.1, 0.2, . . ., 1 кг конфет.

    /*
    float A;

    cout << "Введите стоимость кг конфет =";
    cin >> A;

    for (int count = 1; count <= 10; count++) {
        cout << "Стоимость " << count << "кг. конфет " << count * A << "\n";
    }
    */


    // --------------
    // For6. Дано вещественное число — цена 1 кг конфет. Вывести стоимость 1.2,1.4, . . ., 2 кг конфет.

    /*
    float A, price = 1;

    cout << "Введите стоимость кг конфет =";
    cin >> A;

    for (int count = 1; count <= 5; count++) {
        price += 0.2;
        cout << "Стоимость " << price << "кг. конфет " << A * price << "\n";
    }
    */



    // --------------
    // For7. Даны два целых числа A и B (A < B). Найти сумму всех целых чисел от A до B включительно.

    /*
    int A, B, count = 0;

    cout << "Введите целое число A=";
    cin >> A;

    cout << "Введите целое число которое больше А B=";
    cin >> B;

    for (A; A <= B; A++) {
        count += A;
        cout << "\nСумма чисел от A до B включительно = "  << count;
    }
    */


    // --------------
    // For8. Даны два целых числа A и B (A < B). Найти произведение всех целых чисел от A до B включительно.

    /*
    int A, B, count = 1;

    cout << "Введите целое число A=";
    cin >> A;

    cout << "Введите целое число которое больше А B=";
    cin >> B;

    for (A; A <= B; A++) {
        count *= A;
        cout << "\nСумма чисел от A до B включительно = " << count;
    }
    */


    // --------------
    // For9.
    // Даны два целых числа A и B (A < B). Найти сумму квадратов всех целых
    // чисел от A до B включительно
    
    /*
    int A, B, count = 0;

    cout << "Введите целое число A=";
    cin >> A;

    cout << "Введите целое число которое больше А B=";
    cin >> B;


    for (A; A <= B; A++) {
        count += (A * A);
    }
    cout << "\nСумма чисел от A до B включительно = " << count;
    */


    
    

    // --------------
    // For10.
    //  Дано целое число N (> 0). Найти сумму
    //  1 + 1/2 + 1/3 + . . . + 1/N

    /*
    float N, A = 2, count = 1;

    cout << "Введите целое число N больше нуля =";
    cin >> N;

    for (N; A <= N; A++) {
        count += (1/A);
    }
    cout << "\nРезультат = " << count;
    */


    // --------------
    // For11.
    // Дано целое число N (> 0). Найти сумму
    // N^2 + (N + 1)^2 + (N + 2)^2 + . . . + (2·N)^2

    
    /*
    int N, A = 0;

    cout << "Введите целое число N больше нуля =";
    cin >> N;

    int  count = N * N;

    for (A; A <= N; A++) {
        count += (N + A) * (N + A);
    }

    cout << "\nРезультат = " << count;
    */


    // --------------
    // For13.
    // Дано целое число N (> 0). Найти значение выражения
    // 1.1 − 1.2 + 1.3 − . . .

    /*
    int N; 
    float A = 1, count = 0, mod = 1, temp;

    cout << "Введите целое число N больше нуля =";
    cin >> N;

    for (A; A <= N; A = A + 0.1) {
        temp = mod * A;
        mod = mod * -1;
        count += temp;
        cout << "\n= " << count;
    }

    cout << "\nРезультат = " << count;
    */



    // --------------
    // For15.
    // Дано вещественное число A и целое число N (> 0). Найти A в степени N:
    // A^N = A·A· . ..·A
    // (числа A перемножаются N раз)
    
    /*
    int N;
    float A, count, sum = 1;

    cout << "Введите число А=";
    cin >> A;

    cout << "Введите целое число N больше нуля =";
    cin >> N;


    for (count = 0; count <= N; count++) {
        sum *= A;
    }
    cout << "\nРезультат = " << sum;
    */



    // --------------
    // For17.
    // Дано вещественное число A и целое число N (> 0). Используя один цикл, найти сумму
    // 1 + A + A^2 + A^3 + . . . + A^N
    
    /*
    int N;
    float A, count, sum = 1;
    cout << "Введите число А=";
    cin >> A;
    cout << "Введите целое число N больше нуля =";
    cin >> N;
    for (count = 0; count <= N; count++) {
        sum += pow(A, count);
    }
    cout << "\nРезультат = " << sum;
    */


    // --------------
    // For19.
    // Дано целое число N (> 0). Найти произведение N! = 1·2·. . .·N
    // (N–факториал). Чтобы избежать целочисленного переполнения, вычислять это произведение 
    // с помощью вещественной переменной и вывести его как вещественное число.

    /*
    float sum = 1;
    int count = 1, N;

    cout << "Введите целое число N больше нуля =";
    cin >> N;

    for (count; count <= N; count++) {
        sum *= count;
    }
    
    cout << "\nРезультат = " << sum;
    */


    // --------------
    // For21.
    // Дано целое число N (> 0). Используя один цикл, найти сумму
    // 1 + 1/(1!) + 1/(2!) + 1/(3!) + . . . + 1/(N!)
    // (выражение N! — N–факториал — обозначает произведение всех целых
    // чисел от 1 до N : N!= 1·2·. ..·N).Полученное число является приближенным значением константы e = exp(1).

    /*
    int N, count;
    float sum = 0, temp = 1;

    cout << "Введите целое число N больше нуля =";
    cin >> N;

    for (count = 1; count <= N + 1; count++) {
        temp *= count;
        sum += 1 / (temp);
    }

    cout << "\nРезультат = " << sum;
    */


    // --------------
    // For23.
    // Дано вещественное число X и целое число N (> 0). Найти значение выражения
    // X − X^3/ (3!) + X^5/ (5!) − . . . + (−1)^N ·X^(2·N) + 1 / ((2·N + 1)!)
    // Полученное число является приближенным значением функции sin в точке X.
    
    /*
    float X; 
    int N, mod = -1;

    cout << "Введите вещественное число X =";
    cin >> X;

    cout << "Введите целое число N больше нуля =";
    cin >> N;

    float sum = X;

    for (int count = 3; count <= N + 1; count = count + 2) {
        int temp = 1;
        for (int i = 1; i <= count; i++) {
            temp *= i;
        }
        
        sum += ((pow(X, count)/ temp) * mod);
        mod *= -1;
    }

    cout << "\nРезультат = " << sum;
    */

    

    // --------------
    // For25.
    // Дано вещественное число X (|X| < 1) и целое число N (> 0). Найти значение выражения
    // X − X^2/ 2 + X^3/ 3 − . . . + (−1)^(N−1)·X^N / N
    // Полученное число является приближенным значением функции ln в точке 1 + X

    float X, sum;
    int N, mod = -1;

    cout << "Введите вещественное число X меньше единицы но больше нуля=";
    cin >> X;

    cout << "Введите целое число N больше нуля =";
    cin >> N;
    sum = X;
    for (int count = 2; count <= N; count++) {
        sum += (pow(X, count) / 2) * mod;
        mod *= -1;
    }
    cout << "\nРезультат = " << sum;

}

